[package]
name = "quew-stdlib"
version = "0.1.0"
edition = "2024"
description = "Quew standard library â€” native builtin implementations."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
inventory = { workspace = true }
quew-macros = { path = "../quew-macros" }
quew-runtime = { path = "../quew-runtime" }
reqwest = { version = "0.12", features = ["blocking", "rustls-tls"], default-features = false }
serde_json = "1.0"
indexmap = { workspace = true }

[build-dependencies]
