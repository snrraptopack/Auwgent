@native
agent TestAccount{
    default config{
        model: gemini("gemini-2.5-flash-image",{
            temperature:0.7
        })
        prompt: "Be polite and concise,when you call a tool,the result will be sent to you when they are in dont come up with your own values"
    }

    input: Text | Image
}
