i realized the prompt filtering wasnt taking into account when it just conditional...
first of what is it when we use the ctx. property sometimes we are referencing them on some portion of the codebase and we dont want duplicate it in the original default area that will say #additional context and list them.... howerver this also remove it when we just doing a conditional check to render something different and not the ctx.property itself example



You are a helpful assistant
"not that old"



# ADDITIONAL CONTEXT
user_name: Amihere
id: '10' this result here was supposed to show the age again the not that old was a conditional stuf we did  You are a helpful assistant
        {{#if ctx.age > 20}}
            "The person is old"
        {{else}}
            "not that old"
        {{/if}}

        and even when i do this i dont get the ctx.age rendered   {{#if ctx.age > 20}}
            "The person is old {{ctx.age}}"
        {{else}}
            "not that old"
        {{/if}}

        and one critical bug  You are a helpful assistant
        {{#if ctx.age > 20}}
            "The person is old {{ctx.age}}"
        {{else}}
            "not that old"
        {{/if}}         rhe result even without the condtion should present the you are helpful assistant howerver when i do something that will result the ctx.age into truthy  the you are helpful nor the "The person is old get rendered"


The issue i noticed it the compiled prompt doesnt show the helper input they accept in the model facing..Helpers:
- Joker // Is A Joker Helper use it for jokes
- Plan // Is a helper specialized at planning and decomposing tasks
- Fact // Is a helper for checking fact just this has to show the args or params they take...

i realized the codegen wasnt taking into account the to generate the intent well example..
sers\babyface\Desktop\auwgent\Auwgent\targets\rust\rust-verfication/test_case/main.agent.rs:545:15
    |
545 |         match intent.raw() {
    |               ^^^^^^^^^^^^ pattern `&AuwgentIntent::Loud(_)` not covered
    |
note: `AuwgentIntent` defined here
   --> C:\Users\babyface\Desktop\auwgent\Auwgent\targets\rust\rust-verfication/test_case/main.agent.rs:325:10
    |
325 | pub enum AuwgentIntent {
    |          ^^^^^^^^^^^^^
...
337 |     Loud(LoudIntent),
    |     ---- not covered
    = note: the matched value is of type `&AuwgentIntent`
help: ensure that all possible cases are being handled by adding a match arm with a wildcard pattern or an explicit pattern as shown
    |
556 ~             AuwgentIntent::HelperResult(value) => self.helper_result(&HelperResults { kind: value.clone() }, agent_name),
557 ~             &AuwgentIntent::Loud(_) => todo!(),
    |

For more information about this error, try `rustc --explain E0004`.
