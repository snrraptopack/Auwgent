import {GeminiModel} from "./models"
agent CHEF{

    default config{
        model : GeminiModel
        prompt: "You are a helpful assistant"
    }

    input {
        text:string
    }

    tool tool_get_user_name():string @desc "used to get the user name"
}