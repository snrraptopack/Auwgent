[package]
name = "quew-codegen"
version = "0.1.0"
edition = "2024"
description = "Type-only stub generators (TypeScript, Python, Dart, Rust) for quew DSL types."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
quew-errors = { path = "../quew-errors" }
quew-interner = { path = "../quew-interner" }
quew-ir = { path = "../quew-ir" }

[dev-dependencies]
insta = { workspace = true }
