[package]
name = "quew-unify"
version = "0.1.0"
edition = "2024"
description = "Type variable unification for the quew type system. Wraps ena's union-find."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
ena = { workspace = true }
quew-errors = { path = "../quew-errors" }
quew-types = { path = "../quew-types" }

[dev-dependencies]
insta = { workspace = true }
