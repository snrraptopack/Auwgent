as part of the  ongoing discussion

where to do something like

let value = "....".isEmpty() we have to extend the string
this is valid and concrete approach and i think this can help in building the request layer for the ai agent itself

extend string {
    #rust("std.string.is_empty")
    function isEmpty(): bool

    #rust("std.string.trim")
    function trim(): string
}


example in the V1 when we do something like

agent Hello {
    default config{
        model:gemini("gemini-2.5-flash")
        prompt: "..."
    }
}

at runtime the sdk interpret the ir to see what provider we ar using
this case it gemini what model do we want this case it 2.5 flash and the rust sdk layer will
route these through reqwest pass the url automatcally and then we extract the fields or whatever we need from the response like the model response token usage error messages and so on so it was more like if we want to change something we need to strictly change it in the rust layer... you can look at it here
"C:\Users\babyface\Desktop\auwgent\Auwgent\runtime\crates\auwgent-drivers\src\gemini.rs"

but now we are moving away from that, we are moving to a phase where the language can write it own standard lib including exposing some sort of io mecahnism where we can have a standalone or top level functions wrapping aroud them to make a request and stuff and all these will be understandtood by the model..

so i ask is it possible to move this gemini stuff from strictly being a rust backed we need to touch it before we can make changes to quew itself through io and other operation to expose the result and stuff we need?

such that when someone write a logic like

agent Hello(input:string){
    reply(input) with {
        model: gemini("...")
        prompt: "..."
    }
}

this time the gemini reponse the gemini is standalone function we created as built in but has a type model

where we can specify the url it should make to the body which we know the runtime can pick the prompt there the fields that needs to  be returned and so on? this will essentially make us to do thing fast and extend the language fast
